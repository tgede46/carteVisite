import 'package:flutter/cupertino.dart';

class Card extends StatelessWidget {
  const Card({super.key});

  @override
  Widget build(BuildContext context) {
    return  Center(
      child: Text('creation de la carte'),
    );
  }
}
